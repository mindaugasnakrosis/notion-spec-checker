"""spec-check core: read-only git wrapper, read-only Notion MCP wrapper,
schemas, snapshot, resolver, parser, collectors. Pure-data + IO modules
shared by the rule layer above. Read-only on git AND on Notion.
"""
